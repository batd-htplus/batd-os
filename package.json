{
    "name": "flow",
    "version": "0.1.0",
    "private": true,
    "description": "AI coding orchestration harness: workflow, ledger, checks and approval around coding CLIs.",
    "type": "module",
    "engines": {
        "node": ">=24"
    },
    "bin": {
        "flow": "src/apps/cli/main.ts"
    },
    "scripts": {
        "test": "node --test \"tests/**/*.test.ts\"",
        "typecheck": "tsc -p tsconfig.json"
    },
    "devDependencies": {
        "@types/node": "^24.0.0",
        "typescript": "^5.9.0"
    }
}
